version: "3.9"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - OPENCODE_SERVER_PASSWORD=OpenCode_Evo_2026
    volumes:
      - opencode_data:/root/.local/share/opencode
    restart: unless-stopped

volumes:
  opencode_data:
